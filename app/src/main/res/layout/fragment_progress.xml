<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#BAE3FF"
    android:clipToPadding="true">

    <ImageView
        android:id="@+id/imageHeader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/top_clock_progress"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true" />

    <TextView
        android:id="@+id/titleKoleksi"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Progress"
        android:textSize="30sp"
        android:textColor="#86290A"
        android:fontFamily="@font/poppins_extrabold"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="45dp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewProgressUser"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/titleKoleksi"
        android:layout_marginTop="16dp"
        android:background="@drawable/bottom_app_bar_background"
        android:clipToPadding="false"
        android:paddingTop="36dp"
        android:paddingStart="24dp"
        android:paddingEnd="16dp"
        android:scrollbars="vertical" />

</RelativeLayout>
